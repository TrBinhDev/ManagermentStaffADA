#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../prisma/build/index.js" "$@"
else
  exec node  "$basedir/../../../../prisma/build/index.js" "$@"
fi
# cmd-shim-target=F:/Project/ManagermentStaffADA/node_modules/.pnpm/@prisma+client@7.8.0_prisma_d8ebb34252d6ec62f82c7a3722862e15/node_modules/prisma/build/index.js
